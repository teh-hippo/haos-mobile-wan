#include <tunables/global>

profile ha_cellular_gateway flags=(attach_disconnected,mediate_deleted,complain) {
  #include <abstractions/base>

  capability net_admin,
  capability net_raw,
  network,

  /app/** r,
  /data/** rwk,
  /run/** rwk,
  /proc/** r,
  /sys/class/net/** r,

  /usr/bin/python3 ix,
  /usr/bin/curl rix,
  /usr/sbin/dnsmasq rix,
  /usr/sbin/conntrack rix,
  /sbin/ip rix,
  /sbin/iptables rix,
  /sbin/ip6tables rix,
}
